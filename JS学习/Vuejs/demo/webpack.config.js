var config = {
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "webpack-dev-server --port 8958 --open --config webpack.config.js"
      },
};

//相当于export default config
module.exports = config;